__pycache__/
*.pyc
.pytest_cache/

# uv lockfile is an untracked ephemeral artifact here (adjudicated review).
uv.lock
.DS_Store

# Real vehicle sector images are read from the artifacts zip in tests;
# never commit them.
tests/data/*.bin
tests/data/*.json

# v850 build intermediates (regenerated by build.sh)
shellcode/build/*.elf
shellcode/build/*.o

# Design/planning docs (spec + plan), kept locally, not published
docs/

# Brainstore intermediate artifacts
.brainstore/
brainstore/

# Python tool caches
.mypy_cache/
.ruff_cache/
.hypothesis/
*.egg-info/
dist/

# v850 linker maps
shellcode/build/*.map
